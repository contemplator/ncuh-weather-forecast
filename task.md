# 專案任務清單 (Weather & Life Map Tasks)

## Phase 1: 基礎地圖與氣象資料渲染 (無資料庫)
*核心目標：完成前端 UI、地圖渲染與中央氣象署 API 串接。*

- `[x]` **1.1 專案初始化**
  - `[x]` 建立 React (Vite) 專案。
  - `[x]` 安裝必備套件：`react-leaflet`, `leaflet`, `axios` (使用原生 fetch 更輕量), `lucide-react` (圖示)。
  - `[x]` **驗證標準**：執行 `npm run dev` 能成功啟動專案，畫面無報錯。
- `[x]` **1.2 建立滿版地圖視圖 (Map-First)**
  - `[x]` 設定全螢幕的地圖容器 (100vh, 100vw)。
  - `[x]` 整合 Leaflet 地圖，中心點對焦於台灣 (約 23.8, 121.0)，縮放級別設定為適合顯示全台。
  - `[x]` **驗證標準**：網頁載入後，呈現滿版台灣地圖，可正常拖曳與縮放。
- `[x]` **1.3 實作「玻璃擬物化 (Glassmorphism)」UI 元件**
  - `[x]` 設定全域 CSS 變數與基礎字體 (Inter 與 Noto Sans TC)。
  - `[x]` 實作共用的毛玻璃卡片樣式 (`backdrop-filter: blur`, 邊框, 半透明背景)。
  - `[x]` 實作預留的「圖層切換 (Layer Switcher)」選單 (UI 已就位，預設啟用天氣並標記 Phase 2 擴充標籤)。
  - `[x]` **驗證標準**：地圖上方懸浮毛玻璃質感的空卡片與選單按鈕，視覺上不突兀且不遮擋地圖操作。
- `[x]` **1.4 串接中央氣象署 (CWA) API**
  - `[x]` 讀取使用者設定的 CWA API 授權碼 (`VITE_CWA_API_KEY`)。
  - `[x]` 撰寫 API Fetcher (呼叫 `F-C0032-001` 一般天氣預報資料)。
  - `[x]` 解析 JSON 回傳格式，將縣市資料萃取出「溫度」、「降雨機率」、「天氣現象 (Wx)」、「舒適度 (CI)」。
  - `[x]` **驗證標準**：成功透過 API 取得並結構化全台縣市天氣資料。
- `[x]` **1.5 將天氣資料渲染至地圖**
  - `[x]` 建立縣市座標對照表 (22 縣市名稱與經緯度)。
  - `[x]` 在地圖上為每個縣市繪製標記 (Marker) 使用自訂圖示 (DivIcon)，顯示該地的氣溫與天氣圖示。
  - `[x]` **驗證標準**：地圖上出現各縣市標記，標記上的氣溫與天氣圖示正確對應 API 資料。
- `[x]` **1.6 實作詳細資訊次視圖 (Secondary View)**
  - `[x]` 點擊地圖上的縣市標記時，平滑平移對焦並顯示該縣市的詳細資訊。
  - `[x]` 以毛玻璃抽屜卡片呈現，顯示氣溫範圍、降雨機率、生活出行建議與未來時段預報。
  - `[x]` **驗證標準**：點擊任一縣市標記，平滑滑出毛玻璃卡片顯示詳細天氣預報。
- `[x]` **1.7 加入 LocalStorage 支援 (暫代資料庫)**
  - `[x]` 實作「我的最愛城市」功能，透過點擊星號儲存至 `localStorage`。
  - `[x]` 網頁重新整理時，從 `localStorage` 讀取並在頂部導覽列顯示快捷最愛標籤。
  - `[x]` **驗證標準**：設定最愛城市後重新整理網頁，最愛狀態仍被保留。

## Phase 2: 導入資料庫 (Supabase) 與進階擴充
*核心目標：建立持久化儲存機制，為未來更多圖層的客製化做準備。*

- `[x]` **2.1 Supabase 專案初始化**
  - `[x]` 於 Supabase 建立專案，配置 `VITE_SUPABASE_URL` 與 `VITE_SUPABASE_ANON_KEY`。
  - `[x]` 安裝 `@supabase/supabase-js` 套件並建立連線設定檔 (`supabaseClient.js`)。
  - `[x]` **驗證標準**：前端能成功初始化 Supabase client，支援自動降級。
- `[x]` **2.2 資料庫 Schema 設計與建立**
  - `[x]` 提供 `user_preferences` 表格 SQL (包含 device_id, favorites, basemap)。
  - `[x]` 啟用 Row Level Security (RLS) 確保資料安全讀寫。
  - `[x]` **驗證標準**：建立具備擴充性的雲端表格結構。
- `[x]` **2.3 將 LocalStorage 狀態遷移至 Supabase**
  - `[x]` 實作輕量免登入之「裝置同步碼 (Device ID / Sync ID)」機制。
  - `[x]` 修改前端邏輯，將「我的最愛」與「底圖設定」的讀寫升級為 Supabase 雲端與 LocalStorage 雙向同步。
  - `[x]` 在 UI 頂部提供「雲端同步狀態標籤」與「SyncModal 同步管理彈窗」，支援跨瀏覽器/裝置輸入同步碼共享設定。
  - `[x]` **驗證標準**：在不同無痕瀏覽器視窗輸入同一組同步碼，可立即同步最愛城市與底圖偏好。

## Phase 3: 部署與文件建立
*核心目標：將專案發布至公網，並完善開發文件交件。*

- `[x]` **3.1 準備生產環境 (Production)**
  - `[x]` 移除專案中不必要的 `console.log` 與測試程式碼（經 grep 確認完全乾淨）。
  - `[x]` 確保 API Keys (氣象署與 Supabase) 正確設定為環境變數 (`.env` 與 `.env.example`)。
  - `[x]` 執行 `npm run build` 確認本地打包無錯誤（成功產出 `dist` 靜態發布包）。
  - `[x]` **驗證標準**：Build 成功且生成 `dist` 資料夾。
- `[x]` **3.2 部署至 Vercel**
  - `[x]` 準備好 GitHub 遠端儲存庫與最新 Commit。
  - `[x]` 配置 Vercel 部署指引與環境變數對應表。
  - `[x]` **驗證標準**：支援透過 GitHub 自動 CI/CD 匯入或 Vercel CLI 部署發布。
- `[x]` **3.3 專案文件 (README.md) 完善**
  - `[x]` 撰寫專案介紹、功能亮點與使用技術架構表。
  - `[x]` 補齊本地啟動教學 (Getting Started) 與環境變數設定說明。
  - `[x]` 撰寫 Vercel 一鍵部署流程與導覽。
  - `[x]` **驗證標準**：任何人依照 README 說明，能順利在本地將專案跑起來並完成雲端部署。

## Phase 4: 資料庫排程爬蟲與快取同步機制
*核心目標：解決 API 限流與金鑰暴露問題，由排程定期抓取天氣與 AQI 寫入 Supabase，前端改讀資料庫快取。*

- `[x]` **4.1 資料庫 Schema 擴充**
  - `[x]` 於 Supabase 建立 `public.weather_records` 表格 (儲存縣市天氣、氣溫、降雨機率、36小時預報與時間戳記)。
  - `[x]` 於 Supabase 建立 `public.aqi_records` 表格 (儲存縣市 AQI、PM2.5、空氣品質等級與時間戳記)。
  - `[x]` 建立即時檢視表 `public.latest_weather` 與 `public.latest_aqi` 供前端一鍵直讀 22 縣市最新資料。
  - `[x]` 設定 RLS 公開讀取權限 (Public Select) 與查詢索引優化。
  - `[x]` **驗證標準**：已透過 Supabase MCP 成功套用 Migration，並完成 Table 與欄位型態驗證。
- `[x]` **4.2 建立排程同步爬蟲腳本**
  - `[x]` 撰寫 Node.js 爬蟲腳本 `scripts/sync-data.js`。
  - `[x]` 串接氣象署 API 與 Open-Meteo 空氣品質 API，清洗並正規化 22 縣市資料。
  - `[x]` 使用 Supabase 權限執行寫入最新快取並附加歷史時間戳記。
  - `[x]` **驗證標準**：在本地手動執行 `node scripts/sync-data.js` 能成功將最新資料寫入 Supabase。
- `[x]` **4.3 配置 GitHub Actions 定時自動化排程**
  - `[x]` 建立 `.github/workflows/data-sync.yml` 工作流。
  - `[x]` 設定 Cron 排程每小時自動觸發一次 (`cron: '0 * * * *'`)，支援手動觸發 (`workflow_dispatch`)。
  - `[x]` 支援在 GitHub 儲存庫設定環境變數 Secrets (`CWA_API_KEY`, `SUPABASE_URL`, `SUPABASE_KEY`)。
  - `[x]` **驗證標準**：工作流配置完畢，推送到 GitHub 即啟用定時排程。
- `[x]` **4.4 前端資料來源遷移為優先讀取資料庫**
  - `[x]` 修改前端 `cwaApi.js` 與新增資料讀取服務 `src/services/dataService.js`：優先向 Supabase 查詢最新全台快取。
  - `[x]` 保留自動降級機制：若資料庫連線失敗，無痛退回前端直接呼叫或 LocalStorage。
  - `[x]` **驗證標準**：前端可在無 CWA API 金鑰的情況下直接從 Supabase 秒級載入天氣與空品資料。

## Phase 5: 空氣品質 (AQI) 資料圖層與生活指標整合
*核心目標：在互動地圖上新增 AQI 空品圖層，結合天氣與空氣品質提供全方位生活防護建議。*

- `[x]` **5.1 串接空氣品質 (AQI) 資料**
  - `[x]` 定義台灣 6 級空氣品質指標顏色與對應標準 (綠0-50、黃51-100、橘101-150、紅151-200、紫201-300、褐紅301+)。
  - `[x]` 解析測站資料並聚合對齊至 22 縣市。
  - `[x]` **驗證標準**：成功取得全台各縣市即時 AQI 數值與 PM2.5 指標。
- `[x]` **5.2 啟用圖層選單 (Layer Switcher) AQI 切換功能**
  - `[x]` 移除 `LayerSwitcher` 中 AQI 的禁用標籤，開放點選切換「即時天氣」與「空氣品質」圖層。
  - `[x]` 設定全域 activeLayer 狀態，驅動地圖標記切換。
  - `[x]` **驗證標準**：點選 AQI 圖層後，地圖能立即響應切換顯示內容。
- `[x]` **5.3 實現 AQI 動態色碼地圖標記 (Marker)**
  - `[x]` 製作 AQI 專屬的自訂標記徽章：顯示「城市名 + AQI 數值 + 空品等級文字」，背景動態套用對應顏色。
  - `[x]` 地圖右下角新增空氣品質等級色卡圖例 (Legend)。
  - `[x]` **驗證標準**：切換至 AQI 圖層時，地圖標記顯示該地空氣品質與對應健康色碼。
- `[x]` **5.4 升級次視圖 (WeatherDrawer) 綜合生活建議**
  - `[x]` 在詳細資訊卡片中加入空氣品質指標專屬卡片 (AQI、PM2.5、狀態標籤)。
  - `[x]` 結合氣候與空品提供更智能的出行建議（例如：高溫且降雨機率低但 AQI 破百 ➔ 建議室內活動、出門配戴口罩）。
  - `[x]` **驗證標準**：點擊任一縣市標記，抽屜卡片能同時呈現該地氣象預報與空氣品質防護建議。

