# 專案任務清單 (Weather & Life Map Tasks)

## Phase 1: 基礎地圖與氣象資料渲染 (無資料庫)
*核心目標：完成前端 UI、地圖渲染與中央氣象署 API 串接。*

- `[ ]` **1.1 專案初始化**
  - `[ ]` 建立 React (Vite) 專案。
  - `[ ]` 安裝必備套件：`react-leaflet`, `leaflet`, `axios` (或使用 native fetch), `lucide-react` (圖示)。
  - `[ ]` **驗證標準**：執行 `npm run dev` 能成功啟動專案，畫面無報錯。
- `[ ]` **1.2 建立滿版地圖視圖 (Map-First)**
  - `[ ]` 設定全螢幕的地圖容器 (100vh, 100vw)。
  - `[ ]` 整合 Leaflet 地圖，中心點對焦於台灣 (約 23.5, 121)，縮放級別設定為適合顯示全台。
  - `[ ]` **驗證標準**：網頁載入後，呈現滿版台灣地圖，可正常拖曳與縮放。
- `[ ]` **1.3 實作「玻璃擬物化 (Glassmorphism)」UI 元件**
  - `[ ]` 設定全域 CSS 變數與基礎字體 (如 Inter 或 Noto Sans TC)。
  - `[ ]` 實作共用的毛玻璃卡片樣式 (`backdrop-filter: blur`, 邊框, 半透明背景)。
  - `[ ]` 實作預留的「圖層切換 (Layer Switcher)」選單 (UI only, 先放假資料或僅提供天氣選項)。
  - `[ ]` **驗證標準**：地圖上方懸浮毛玻璃質感的空卡片與選單按鈕，視覺上不突兀且不遮擋地圖操作。
- `[ ]` **1.4 串接中央氣象署 (CWA) API**
  - `[ ]` 註冊 CWA 會員並取得 API 授權碼 (Authorization Key)。
  - `[ ]` 撰寫 API Fetcher (例如呼叫 `F-C0032-001` 一般天氣預報資料)。
  - `[ ]` 解析 JSON 回傳格式，將縣市資料萃取出「溫度」、「降雨機率」、「天氣現象 (Wx)」。
  - `[ ]` **驗證標準**：在瀏覽器 console 或 Network tab 中成功印出結構化的全台縣市天氣資料。
- `[ ]` **1.5 將天氣資料渲染至地圖**
  - `[ ]` 建立縣市座標對照表 (Mapping table：縣市名稱 -> 經緯度)。
  - `[ ]` 在地圖上為每個縣市繪製標記 (Marker) 或自訂圖示 (DivIcon)，顯示該地的氣溫與天氣圖示 (例如太陽、雨傘)。
  - `[ ]` **驗證標準**：地圖上出現各縣市標記，標記上的氣溫與天氣圖示正確對應 API 資料。
- `[ ]` **1.6 實作詳細資訊次視圖 (Secondary View)**
  - `[ ]` 點擊地圖上的縣市標記時，觸發狀態更新，顯示該縣市的詳細資訊。
  - `[ ]` 以毛玻璃側邊欄 (Sidebar) 或彈出視窗 (Popup) 呈現，顯示包含降雨機率、風速等更多細節。
  - `[ ]` **驗證標準**：點擊台北市標記，會平滑滑出一個毛玻璃卡片，顯示台北市的詳細天氣預報。
- `[ ]` **1.7 加入 LocalStorage 支援 (暫代資料庫)**
  - `[ ]` 實作「我的最愛城市」功能，透過點擊星號儲存至 `localStorage`。
  - `[ ]` 網頁重新整理時，從 `localStorage` 讀取並自動對焦或高亮最愛城市。
  - `[ ]` **驗證標準**：設定最愛城市後重新整理網頁，最愛狀態仍被保留。

## Phase 2: 導入資料庫 (Supabase) 與進階擴充
*核心目標：建立持久化儲存機制，為未來更多圖層的客製化做準備。*

- `[ ]` **2.1 Supabase 專案初始化**
  - `[ ]` 於 Supabase 建立新專案，取得 Project URL 與 API Key。
  - `[ ]` 安裝 `@supabase/supabase-js` 套件並建立連線設定檔。
  - `[ ]` **驗證標準**：前端能成功初始化 Supabase client。
- `[ ]` **2.2 資料庫 Schema 設計與建立**
  - `[ ]` 建立 `user_preferences` 表格 (紀錄使用者 ID、最愛城市陣列、圖層顯示設定)。
  - `[ ]` 設定 Row Level Security (RLS) 確保資料存取安全 (可先設為 public 方便測試，後續加上 Auth)。
  - `[ ]` **驗證標準**：在 Supabase Dashboard 能看到建立好的 Table 與欄位。
- `[ ]` **2.3 將 LocalStorage 狀態遷移至 Supabase**
  - `[ ]` 實作匿名登入 (Anonymous Login) 或簡單的 Email 登入。
  - `[ ]` 修改前端邏輯，將「我的最愛」與「圖層設定」的讀寫從 LocalStorage 改為呼叫 Supabase API。
  - `[ ]` **驗證標準**：在不同無痕瀏覽器視窗登入同一帳號，能同步看到相同的最愛城市與介面設定。

## Phase 3: 部署與文件建立
*核心目標：將專案發布至公網，並完善開發文件交件。*

- `[ ]` **3.1 準備生產環境 (Production)**
  - `[ ]` 移除專案中不必要的 `console.log` 與測試程式碼。
  - `[ ]` 確保 API Keys (氣象署與 Supabase) 正確設定為環境變數 (`.env`)。
  - `[ ]` 執行 `npm run build` 確認本地打包無錯誤。
  - `[ ]` **驗證標準**：Build 成功且生成 `dist` 資料夾。
- `[ ]` **3.2 部署至 Vercel**
  - `[ ]` 將專案推送至 GitHub 儲存庫。
  - `[ ]` 在 Vercel 連結 GitHub 儲存庫並匯入專案。
  - `[ ]` 在 Vercel 設定環境變數。
  - `[ ]` **驗證標準**：獲得 Vercel 提供的公開網址 (例如 `xxx.vercel.app`)，且任何人皆可存取並正常看到氣象資料。
- `[ ]` **3.3 專案文件 (README.md) 完善**
  - `[ ]` 撰寫專案介紹、功能亮點與使用技術。
  - `[ ]` 放入 Live Demo 網址。
  - `[ ]` 補齊本地啟動教學 (Getting Started) 與環境變數設定說明。
  - `[ ]` **驗證標準**：任何人依照 README 說明，能順利在本地將專案跑起來。
